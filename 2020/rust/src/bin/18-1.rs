use std::fs;

fn main() {
    let input = fs::read_to_string("../inputs/18.txt").expect("Couldn't read the file");
    println!(
        "I'm to stupid for this so I will come back wenn I know how to work with binary trees (or atleast I think that is what I'm missing)"
    )
}

